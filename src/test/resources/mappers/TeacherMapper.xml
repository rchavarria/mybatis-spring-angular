<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="es.rchavarria.library.persistence.TeacherMapper">

    <resultMap id="teacherResult" type="es.rchavarria.library.domain.Teacher">
        <id column="idTeacher" property="idTeacher" javaType="long"/>
        <result column="name" property="name" javaType="string"/>
    </resultMap>

    <select id="list" resultMap="teacherResult">
        SELECT * FROM teachers
    </select>
</mapper>